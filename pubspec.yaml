name: moviechallenge
description: Small app that list the most popular movies and now playing movies

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  http: ^1.1.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^1.1.2
  build_runner: ^2.1.10
  test: 1.24.3

  flutter_lints: ^2.0.0


flutter:

  uses-material-design: true

  assets:
    - assets/movie_genres.json
